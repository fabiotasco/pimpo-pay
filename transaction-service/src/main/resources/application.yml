# Spring
spring:
  profiles: default
  application:
    name: transaction-service
  datasource:
    url: jdbc:mysql://us-cdbr-iron-east-01.cleardb.net:3306/heroku_cf29bf85a36d940?useSSL=false
    username: b3358bb98cb89e
    password: 726970d8
    driver-class-name: com.mysql.jdbc.Driver
    tomcat:
      test-while-idle: true
      validation-query: SELECT 1
    initialization-mode: always
    continue-on-error: true
    platform: mysql
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
    hibernate:
      naming:
        physical-strategy: org.springframework.boot.orm.jpa.hibernate.SpringPhysicalNamingStrategy
      ddl-auto: none
      use-new-id-generator-mappings: false
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: America/Sao_Paulo
  # Spring Cloud tem conflitos de referência circular com Spring Data Source
  # https://github.com/spring-cloud/spring-cloud-commons/issues/355
  cloud:
    refresh:
      refreshable: none

# Server
server:
  port: 9030
  use-forward-headers: false
  servlet:
    context-path: /transactions

# Eureka
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
      
# Clients
api:
  client:
    auth-service:
      url: localhost:9000/auth
    account-service:
      url: localhost:9010/accounts
    transaction-service:
      url: localhost:9030/transactions

# Log
logging:
  level:
    ROOT: info
    org.springframework.web: info
    org.hibernate.SQL: info
    org.hibernate.type.descriptor.sql.BasicBinder: trace
    pay.pimpo: debug
  file:  /tmp/logs/transaction-service.log
